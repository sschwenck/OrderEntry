@model OrderEntry.Models.ShipTo

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ShipTo</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.ShipToID)

        <div class="form-group">
            @Html.LabelFor(model => model.ShipToNo, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ShipToNo)
                @Html.ValidationMessageFor(model => model.ShipToNo)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ShipToName, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ShipToName)
                @Html.ValidationMessageFor(model => model.ShipToName)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
