@model IEnumerable<OrderEntry.Models.ShipTo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShipToNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShipToName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShipToNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShipToName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShipToID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShipToID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShipToID })
        </td>
    </tr>
}

</table>
