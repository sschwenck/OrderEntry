@model IEnumerable<OrderEntry.Models.Warehouse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WarehouseNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WarehouseName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WarehouseNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WarehouseName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WarehouseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WarehouseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WarehouseID })
        </td>
    </tr>
}

</table>
